function NotFound() {
    return <div>Page Not Found. Be sure to include to localhost with the path "/stations" in the url,
        and a station name if you want to view a specific station.</div>;
}

export default NotFound;